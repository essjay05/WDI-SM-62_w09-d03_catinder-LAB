<div class="container">
<h1>List of Cats:</h1>

    <% @cats.each do |c| %>
        <%= image_tag (c.image_uri) %>
        <h3><a href="/cats/<%= c.id %>"><%= c.name %></a></h4>
        <p> Votes: <%= c.votes %> </p>
        
    <% end %>
</div>

